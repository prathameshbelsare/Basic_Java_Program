
		//fount ele in Arrays
		
		int a[]= {1,2,3,4,5,6,7,8,9};
		int n =8,count=0;
		for(int i=0; i<a.length; i++) {
			if(a[i]==n) {
				count++;
			}
		}if(count>0) {
			System.out.println("Fount");
		}else {
			System.out.println("Not Fount");
		}
